name: Browser E2E

on:
  push:
    paths:
      - 'frontend/**'
      - 'SkinCare/**'
      - 'e2e/**'
      - '.github/workflows/e2e-ci.yml'
  pull_request:
    paths:
      - 'frontend/**'
      - 'SkinCare/**'
      - 'e2e/**'
      - '.github/workflows/e2e-ci.yml'

permissions:
  contents: read

jobs:
  browser-e2e:
    runs-on: ubuntu-24.04
    timeout-minutes: 20

    services:
      postgres:
        image: postgres:18
        env:
          POSTGRES_DB: sharafi_e2e
          POSTGRES_USER: sharafi
          POSTGRES_PASSWORD: sharafi_e2e_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U sharafi -d sharafi_e2e"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      APP_ENV: testing
      APP_DEBUG: 'false'
      APP_URL: http://127.0.0.1:8000
      APP_KEY: 'base64:MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY='
      OTP_PEPPER: 'ci-only-e2e-otp-pepper-that-is-at-least-32-characters'
      SMS_DRIVER: e2e
      PAYMENT_DRIVER: e2e
      PAYMENT_CALLBACK_URL: http://127.0.0.1:8000/api/v1/payments/zarinpal/callback
      PAYMENT_RESULT_URL: http://127.0.0.1:8000/payment-result.html
      DB_CONNECTION: pgsql
      DB_HOST: 127.0.0.1
      DB_PORT: 5432
      DB_DATABASE: sharafi_e2e
      DB_USERNAME: sharafi
      DB_PASSWORD: sharafi_e2e_password
      DB_SSLMODE: disable
      CACHE_STORE: array
      QUEUE_CONNECTION: sync
      SESSION_DRIVER: database
      SESSION_SECURE_COOKIE: 'false'
      SESSION_SAME_SITE: lax
      SANCTUM_STATEFUL_DOMAINS: 127.0.0.1:8000
      CORS_ALLOWED_ORIGINS: http://127.0.0.1:8000
      E2E_BASE_URL: http://127.0.0.1:8000

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: '8.5'
          extensions: mbstring, openssl, pdo_pgsql
          coverage: none
          tools: composer:v2

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: e2e/package-lock.json

      - name: Install backend dependencies
        working-directory: SkinCare
        run: composer install --no-interaction --prefer-dist --no-progress

      - name: Audit backend dependencies
        working-directory: SkinCare
        run: composer audit --locked

      - name: Install locked E2E dependencies
        working-directory: e2e
        run: npm ci --ignore-scripts --no-audit --no-fund

      - name: Audit E2E dependencies
        working-directory: e2e
        run: npm audit --audit-level=high

      - name: Install Chromium
        working-directory: e2e
        run: npx playwright install --with-deps chromium

      - name: Prepare database and deterministic fixtures
        working-directory: SkinCare
        run: |
          php artisan migrate:fresh --force
          php artisan db:seed --class=E2ESeeder --force
          rm -rf storage/framework/e2e
          mkdir -p storage/framework/e2e
          chmod 700 storage/framework/e2e

      - name: Mount storefront into Laravel public root
        run: rsync -a frontend/ SkinCare/public/

      - name: Start Laravel test server
        working-directory: SkinCare
        run: |
          php artisan serve --host=127.0.0.1 --port=8000 > storage/logs/e2e-server.log 2>&1 &
          echo $! > /tmp/sharafi-e2e-server.pid
          for attempt in {1..30}; do
            if curl --fail --silent http://127.0.0.1:8000/api/v1/health >/dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat storage/logs/e2e-server.log
          exit 1

      - name: Run Playwright commerce and responsive suite
        working-directory: e2e
        run: npm test -- --grep-invert @visual

      - name: Upload failure diagnostics
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: sharafi-e2e-failure
          if-no-files-found: ignore
          retention-days: 7
          path: |
            e2e/test-results/
            SkinCare/storage/logs/e2e-server.log
